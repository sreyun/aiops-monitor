# AIOps · 系统完善度评估与后续方向（第一阶段）

> 评估日期：2026-07-19
> 方法：三个并行代码探索代理（very thorough 只读）分别审查 Go 后端、安卓客户端、前端/营销站；主代理综合并交叉核验关键事实。
> 范围：全量源码（含 `cmd/server` 126 个 Go 文件 / 40k+ 行、`cmd/agent` 69 个 / 18k+ 行、`shared`；`android/` 17k+ 行 Kotlin；`website/` 6 页 + i18n 三语）。

---

## 一、总体结论

| 域 | 成熟度定级 | 一句话结论 |
|---|---|---|
| Go 后端 | **成熟（生产可用）** | 监控闭环 + AI/RAG 诊断在代码层已端到端打通，安全与韧性设计高于一般自研 |
| 安卓客户端 | **A-（生产级）** | 实际为 20+ 屏企业级移动控制台，**项目记忆对其"MVP/无图表/无推送"的描述已严重过时** |
| 前端/营销站 | **良好（B+）** | i18n 三语架构健康，但存在数处 CJK 硬编码违规与"无 JS 白屏"健壮性缺口 |

**整体判断**：系统已具备生产可用性，属"可用但待完善"级别，无"缺失"级阻断。最大风险点集中在三处：① 横向授权粒度（`/proxy` 按主机/端口 ACL）；② 营销站 CJK 硬编码违规与无 JS 白屏；③ 项目文档/记忆与真实代码严重脱节（尤其安卓）。

---

## 二、Go 后端评估（成熟）

**已成熟实现（代码层端到端打通，非占位）：**
- 双强制存储：PostgreSQL（关系+审计+JSONB+pgvector `diagnosis_embeddings` RAG）+ VictoriaMetrics（指标），缺任一即 `log.Fatal`
- 监控闭环：`report→check/apimon→alerts→alertgov→incidents→remediation(playbook+审批/护栏)→slos→tickets`
- AI 巡检诊断 + RAG 反馈重排学习闭环（`rerankByFeedback` 👍上浮/👎下沉）
- 鉴权：PBKDF2-HMAC-SHA256 600k 迭代、RBAC 路由矩阵、TOTP 单次使用防重放、终端二次密码 + 限流、IP+账户双维防暴破
- 机器指纹防克隆、安装令牌轮换+7 天宽限、网关中继（防 Host 注入）、多服务端并发广播容灾（断路器+退避+gzip 降级）
- 测试：64 个 `*_test.go`，覆盖 auth/RBAC/TOTP/check/apimon/SLO/playbook/message/RAG 重排等

**待补项（按价值排序）：**
1. **P1** `/proxy` 与 `/forward` 仅按 operator+ 角色放行，**无按主机/端口细粒度 ACL**（已知暴露面，依赖网络隔离）
2. **P2** 短信/语音通知为占位（`auth.go:434` TODO），需接真实 SMS 网关
3. **P2** 迁移无版本化（纯 `CREATE TABLE IF NOT EXISTS`），建议引入 `schema_version` + 增量迁移
4. **P2** VM 写入 best-effort 丢数据无计数/可观测性
5. **P3** AI 诊断生产就绪（密钥管理、重试、嵌入维度 1536 硬编码可配置化）
6. **P3** 清理遗留 `db.go`（`aiops.db` 死代码，全仓无调用）
7. **P3** 自愈 remediation 增加命令白名单/危险命令拦截/执行前快照回滚

---

## 三、安卓客户端评估（A-，远超预期）

> ⚠️ **重要更正**：项目记忆记载"6 屏 MVP / Cookie 内存态 / 终端 MVP / MFA 无 UI / 无图表 / 无推送"——经源码核验**已全部过时**。当前真实状态如下。

**技术栈确认**：Gradle 8.9 / AGP 8.5.2 / Kotlin 1.9.24 / Compose BOM 2024.09.00 / minSdk26 / targetSdk34，与声明一致；目录存在 `aiops-6193.apk`（versionCode 一致），历史上确已成功构建。

**实际能力（源码实证）：**
- **20+ 屏幕/路由**（非 6 屏）：设置、登录（含 MFA 动态口令弹窗）、SRE 驾驶舱总览、主机详情（**原生 Canvas 图表** InteractiveTimeSeriesChart）、告警、企业级 VT 终端、运维中心 SRE Hub（事件闭环/AI 诊断/剧本/SLO/修复审批/工单）、监控拨测、AI 助手（SSE 流式）、硬件/NetFlow/Hyper-V、终端会话回放、消息中心、重复主机清理、告警治理、终端密码、环境切换……
- **Cookie 已持久化**（`DataStore` 双轨：内存 CookieJar + `KEY_SESSION_COOKIE`），冷启动重建会话——推翻"内存态"
- **终端为 A 级**：VT100/UTF-8 解码、指数退避重连、软键盘避让、横竖屏不重建 Activity
- **MFA 登录 UI 已实现**；终端二次密码 UI 已实现（仅自助绑定/全局策略仍在网页端）
- **自建 `/ws/push` 长连接前台服务 + 系统通知**——推翻"无推送"（准确说：无第三方云推送 FCM，依赖自建长连接）
- 网络层扎实：NullSafe 类型适配（防 Go nil NPE）、401 统一拦截、双 Retrofit 实例（AI 读超时 300s）、WS 鉴权桥

**仍存在的真实限制（需在第二阶段文档/营销中如实描述，勿过度承诺）：**
- 沙箱无 SDK/模拟器，**本次为静态评估，未重新编译验证**（APK 存在证明历史可构建，但不保证当前源码零编译错误）
- 账号自服务缺口：MFA 自助绑定(QR)/启用/停用、忘记密码流程、首登强制改密 UI 仍在网页端（首登 admin 在移动端可能卡死）
- 会话 Cookie 用普通 `DataStore` 明文存储，未用 `EncryptedSharedPreferences`（建议对齐后端 AES-256-GCM）
- 固定 10s/15s 轮询无背压；主机/告警列表全量拉取，大环境可能卡顿
- 后端有能力但客户端未覆盖：用户/RBAC 管理、全局 MFA 策略、SNMP/硬件写操作

**后续方向（按价值）：** ① 补齐账号自服务闭环（MFA 绑定/忘记密码/强制改密 UI）② 会话 Cookie 加密存储 ③ 智能刷新策略 ④ 大数据分页 ⑤ 可选 FCM ⑥ 补 UI 测试+CI

---

## 四、前端 / 营销站评估（B+，有清晰优化路径）

### 4.1 代码架构健康度
- **i18n 三语对齐：优秀**。顶层 key（_common16/index118/features13/solutions11/comparison14/faq11/contact48）三语数量完全一致；嵌套动态数据（对比表/7 场景/10 功能组/15 FAQ/8 集成/6 转发要点/4 企业卡）均三语等长。**反向校验：所有 HTML `data-i18n` 引用均能命中，无失效 key、无缺翻译**——这是项目最大工程亮点，须保持。
- **动态渲染 `renderDynamic` 健全**：`getPageName()` 正确分发 6 个渲染函数，13 个容器全部存在、无悬空。
- **响应式/无障碍/双主题/`prefers-reduced-motion`**：均良好（`:focus-visible` 全覆盖、装饰 SVG 自动 `aria-hidden`、无闪烁主题初始化）。
- **CSS 与设计系统一致**：渲染函数输出的类均有样式，无孤儿渲染类。

### 4.2 铁律违规（必须修，P0）
尽管字典干净，以下位置把文案写死，**切换语言后残留中文**，违反"禁止 CJK 硬编码"：
1. `index.html:116-117` 两条社会证明卡无 `data-i18n`（且与 hero-stats 重复，建议删除）
2. `index.html:392/397/400` 端口转发终端样机中文注释硬编码
3. `contact.html:110-111` 订阅输入框 `placeholder` 硬编码（不随语言切换）
4. `js/main.js:125` 移动端吸底 CTA 文本写死
5. `js/main.js:270/297/300` 订阅表单提示（无效/存储失败/成功）硬编码中文，导致 `subscribe.*` 字典项变死 key

### 4.3 健壮性缺口（P1，最高风险）
- **`.reveal{opacity:0}` 导致整站强依赖 JS**：JS 失效/被禁用/爬虫不执行时页面全白（连静态区块也不可见）。建议加 `.js` 守卫类（`<head>` 内联 `documentElement.classList.add('js')`，CSS 改为 `.js .reveal{...}`），no-JS 时内容默认可见——拯救 SEO 与 JS 失败场景。

### 4.4 一致性（P1）
- 死 key：`index.hero.creds`（未渲染）、`index.faq.*`（首页无 FAQ 区块）、`contact.subscribe.placeholder/ok/invalid`（被 main.js 硬编码取代）→ 可启用或清理
- 孤儿样式：`.cta-cmd-inner`、`.float-anim`、`.hero-creds`（可随死 key 启用一石二鸟）

### 4.5 营销内容视角（资深营销专家评估，B+）
**已做对**：首页 Hero 直击痛点、四大痛点卡配解法、"一个二进制替代 5+ 工具栈"价值主张清晰；features 已重组为"客户成效支柱"（痛点→能力→收益+角色 chips）；solutions 7 真实场景；comparison 差异化明确；转化路径连贯（CTA→GitHub）。量化锚点扎实（省 70% 人力、5 分钟定位、3 分钟部署、告警降 80%、1–5000+ 台）。

**可优化（P2，提升转化率与可信度）：**
1. 社会证明 `proof.q1/q2` 为**匿名引言**，B2B 可信度偏低 → 改为具名证言（姓名/角色/公司）+ 可选 logo
2. `proof.usage`"已被用于监控 1–5000+ 台主机"来源模糊 → 补场景/标识
3. 转化断点：所有 CTA 指向 GitHub 自助部署，但企业级区块暗示付费支持/合作，**缺"预约演示/联系销售"入口** → 企业客户被导向 GitHub 而非留资
4. `hero.stat2.label`"0 手动配置"略夸张 → 改为"无需手动改配置"更严谨
5. 可加首页紧凑 FAQ 预告（复用 `index.faq.*` 死 key，提升 SEO/转化）

### 4.6 前端优化方案（优先级 + 分阶段）
| 优先级 | 项 | 影响 | 工作量 |
|---|---|---|---|
| **P0** | 4.2 五项 CJK 硬编码清零 | 三语体验/铁律合规 | 低 |
| **P1** | 4.3 reveal 无 JS 守卫 | SEO/健壮性（最高风险） | 低 |
| **P1** | 4.4 死 key/孤儿样式清理复用 | 可维护性/一致性 | 中 |
| **P2** | 4.5 具名证言/企业 CTA/FAQ 预告/措辞对齐 | 转化率/可信度 | 中–高 |

- **阶段一（1–2 天）合规与健壮性**：P0 全做 + P1 reveal 守卫 → 三语零残留、JS 失效不再白屏
- **阶段二（2–3 天）一致性收敛**：P1 死 key/孤儿样式清理复用
- **阶段三（3–5 天）转化与说服力**：P2 具名证言、企业 CTA、首页 FAQ 预告、措辞对齐

---

## 五、跨域一致性问题（须优先纠正）

1. **项目记忆对安卓的描述严重过时**（"MVP/无图表/无推送/Cookie 内存态"）→ 已在本报告第三节更正；第二阶段 README 与营销内容须按**真实能力**描述，并保留"私有化自托管/未上架应用商店/沙箱未编译验证"的如实边界。
2. **"Hermes" 交付物违规**：网站零残留（合规）；但 `android/README.md` 第 71–72 行出现 `hermes/chat`、`hermes/suggestions` 路由名 → 第二阶段重写文档时须清除。安卓主源码仅一个测试文件引用后端路由名（源码内部，规则允许）。
3. **features 实为 10 组支柱**（非此前纪要所称 8 组），三语各 10，内容自洽；第二阶段若引用数量须写"10"。

---

## 六、后续发展方向路线图（综合三域）

**近期（高价值 / 中可行性）：**
- 后端：`/proxy` 细粒度 ACL（按主机/端口）、短信/语音通知真实落地
- 安卓：账号自服务闭环（MFA 绑定/忘记密码/强制改密 UI）、会话 Cookie 加密
- 营销站：P0/P1 合规与健壮性修复（见 4.6）

**中期（差异化卖点强化）：**
- 后端：迁移版本化、AI 诊断生产就绪（重试/质量评估/维度可配置）
- 安卓：智能刷新、大数据分页、可选 FCM
- 营销站：P2 具名证言 + 企业转化链路（预约演示/留资）

**长期（平台化）：**
- 自愈 remediation 命令白名单/回滚、端到端/混沌测试
- 安卓用户/RBAC 管理移动化
- 统一文档体系与版本化发布说明

---

## 七、第二阶段任务预告（用户已确认分阶段的下一步）

1. **README 重写**（README.md / README_EN.md）：产品定位、能力全景、架构、快速开始、差异化卖点；**按真实安卓能力描述，清除 Hermes，三语同步**
2. **安装/部署文档重写**（INSTALL.md / DEPLOY_GUIDE.md 等）：PG+VM 双存储、服务端/采集端/安卓端、网关中继、多服务端容灾、MFA、机器指纹；**清除 Hermes**
3. **营销网站内容深度重构**（大厂营销专家视角）：客户视角信息架构、价值主张、差异化卖点、转化路径、安卓亮点**按真实能力升级**、落实 P0/P1/P2 优化

> 注：第二阶段"源代码本身暂不修改"——指不改变 Go/安卓应用逻辑；文档与营销站（含 website HTML/CSS/JS 文案）按本评估结论优化。
