Go 1.22（Go 1.22 method+path mux、`log/slog`、`//go:embed`）；PostgreSQL（lib/pq）作为唯一关系型后端；VictoriaMetrics 作为时序存储；Android 端使用 Kotlin + Jetpack Compose；前端为纯原生 HTML/CSS/JS（无框架），通过 Service Worker 实现 PWA；BMC 采集兼容旧固件 TLS 1.0/RSA 套件。