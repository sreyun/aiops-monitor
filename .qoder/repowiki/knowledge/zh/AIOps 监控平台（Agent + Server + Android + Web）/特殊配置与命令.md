生产运行需设置环境变量 `AIOPS_POSTGRES_DSN` 和 `AIOPS_VM_URL`，否则 server 直接退出；版本注入通过 `powershell -File build.ps1` 或 `go build -ldflags "-X main.appVersion=$(git describe --tags)" ./cmd/server ./cmd/agent`；i18n 前端语言包通过 `scripts/build_i18n_en.js` / `build_i18n_tw.js` 从 JSON 源编译为 JS。