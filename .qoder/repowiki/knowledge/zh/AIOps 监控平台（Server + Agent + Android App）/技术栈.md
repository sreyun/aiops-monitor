Go 1.22（`go.mod`）；PostgreSQL + pgvector（关系型持久化，`docker-compose.yml` 使用 `pgvector/pgvector:pg18`）；VictoriaMetrics（时序指标，remote-write 写入）；Android Kotlin/Compose（Jetpack Compose UI + Retrofit API 客户端）；Python 插件 SDK（`plugins/plugin_sdk.py`）；Docker/Docker Compose 一键编排。