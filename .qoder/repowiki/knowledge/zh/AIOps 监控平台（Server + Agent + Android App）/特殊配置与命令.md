本地开发需先 `docker compose up -d` 拉起 postgres 与 victoriametrics，再设置环境变量 `AIOPS_POSTGRES_DSN` 与 `AIOPS_VM_URL` 后运行 server；Windows 下用 `powershell -File build.ps1 [-CrossCompile]` 构建，默认输出到 `bin/`；生产部署参考 `DEPLOY_GUIDE.md` 与 `INSTALL.md`。