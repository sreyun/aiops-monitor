必须同时配置 `AIOPS_POSTGRES_DSN` 和 `AIOPS_VM_URL` 环境变量，否则服务直接退出；生产构建通过 `build.ps1` 或 `go build -ldflags "-X main.appVersion=$(git describe --tags)" ./cmd/server ./cmd/agent` 注入版本号；TLS 通过 `AIOPS_TLS_CERT/AIOPS_TLS_KEY` 启用，未配置则明文 HTTP 并打印警告。