Go 1.22 + `net/http` 原生 mux（无第三方 Web 框架）；PostgreSQL + `lib/pq`（含 pgvector 扩展用于 AI 记忆 RAG）；VictoriaMetrics 作为唯一时序后端；Android 侧 Kotlin + Jetpack Compose + OkHttp；Python 插件生态（`plugins/*.py` + `plugin_sdk.py`）。