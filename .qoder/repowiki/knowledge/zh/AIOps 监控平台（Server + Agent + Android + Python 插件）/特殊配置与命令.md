生产部署必须设置环境变量 `AIOPS_POSTGRES_DSN` 与 `AIOPS_VM_URL`，否则 Server 直接 `log.Fatal` 退出；TLS 通过 `AIOPS_TLS_CERT`/`AIOPS_TLS_KEY` 启用，未配置时仅明文 HTTP 并告警。版本注入通过 `build.ps1` 或 `go build -ldflags "-X main.appVersion=$(git describe --tags)" ./cmd/server ./cmd/agent`。