构建需先安装 Python 环境（Agent 运行 .py 插件），并通过 `powershell -File build.ps1` 或 `go build -ldflags "-X main.appVersion=$(git describe --tags)" ./cmd/server ./cmd/agent` 注入版本；运行时必须设置 `AIOPS_POSTGRES_DSN` 与 `AIOPS_VM_URL` 环境变量，否则 Server 启动即退出。