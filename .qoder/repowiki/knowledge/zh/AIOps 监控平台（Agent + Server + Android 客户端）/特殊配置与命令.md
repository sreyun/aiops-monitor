运行需同时配置 `AIOPS_POSTGRES_DSN` 与 `AIOPS_VM_URL` 两个环境变量，否则 server 启动即报错退出（内置 aiops.db 已停用）；TLS 通过 `AIOPS_TLS_CERT`/`AIOPS_TLS_KEY` 开启 HTTPS；Agent 支持 relay 模式（`--relay --listen :8529 --server <云中心>`）作为网关反向代理。