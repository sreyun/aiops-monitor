Go 1.22（Go module `aiops-monitor`，仅显式依赖 `lib/pq`、`skip2/go-qrcode`、`ledongthuc/pdf`，其余为标准库）；PostgreSQL（`github.com/lib/pq`，启用 pgvector 扩展用于 AI 记忆 RAG）；Android 端 Kotlin + Jetpack Compose；构建产物通过 `build.ps1` 注入 `-ldflags "-X main.appVersion=$(git describe --tags)"` 版本号。